import { FC } from 'react';

import RemoveTokenModal from './index';

export default {
  title: 'components/modals/RemoveTokenModal',
  component: RemoveTokenModal,
};

export const Default: FC = () => <RemoveTokenModal id="" />;
