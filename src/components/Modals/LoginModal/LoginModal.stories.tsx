import { FC } from 'react';

import LoginModal from './index';

export default {
  title: 'components/modals/LoginModal',
  component: LoginModal,
};

export const Default: FC = () => <LoginModal id="" />;
