import { FC } from 'react';

import BurnTokenModal from './index';

export default {
  title: 'components/modals/BurnTokenModal',
  component: BurnTokenModal,
};

export const Default: FC = () => <BurnTokenModal id="" />;
