import React from 'react';

import Button from './index';

export default {
  title: 'components/Button',
  component: Button,
};

export const Default: React.FC = () => {
  return (
    <div>
      <Button />
    </div>
  );
};
