import { FC } from 'react';

import ConnectButton from '.';

export default {
  title: 'containers/header/ConnectButton',
  component: ConnectButton,
};

export const Default: FC = () => <ConnectButton />;
