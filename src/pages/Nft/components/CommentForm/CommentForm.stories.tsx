import { FC } from 'react';

import { CommentForm } from './CommentForm';

export default {
  title: 'nft/CommentForm',
  component: CommentForm,
};

export const Default: FC = () => <CommentForm />;
