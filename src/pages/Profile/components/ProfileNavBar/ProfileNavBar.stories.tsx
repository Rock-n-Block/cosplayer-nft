import { FC } from 'react';

import { ProfileNavBar } from './ProfileNavBar';

export default {
  title: 'profile/ProfileNavBar',
  component: ProfileNavBar,
};

export const Default: FC = () => <ProfileNavBar />;
